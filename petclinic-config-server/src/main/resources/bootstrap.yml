server:
  port: 8012
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:
          default-label: main
          clone-on-start: true
        native:
          search-locations:
  profiles:
    active: native